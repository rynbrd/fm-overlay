diff -urN a/configure.in b/configure.in
--- a/configure.in	2010-04-06 13:05:27.000000000 -0500
+++ b/configure.in	2011-11-30 16:54:59.907017980 -0600
@@ -44,31 +44,38 @@
 AC_SUBST(runtimedir)
 AC_DEFINE_UNQUOTED([SWITCH_RUN_DIR],"${runtimedir}",[where to put pidfile to])
 
-logfiledir="${prefix}/log"
+# Where to put log files
+AC_ARG_WITH([logdir],
+	[AS_HELP_STRING([--with-logdir=DIR], [Put log files into this location (default: $prefix/log)])], [logfiledir="$withval"], [logfiledir="${prefix}/log"])
 AC_SUBST(logfiledir)
 AC_DEFINE_UNQUOTED([SWITCH_LOG_DIR],"${logfiledir}",[where to put log files])
 
-dbdir="${prefix}/db"
+# Where to put local data
+AC_ARG_WITH([datadir],
+	[AS_HELP_STRING([--with-datadir=DIR], [Put data files into this location (default: $prefix)])], [fsdatadir="$withval"], [fsdatadir="${prefix}"])
+AC_SUBST(fsdatadir)
+
+dbdir="${fsdatadir}/db"
 AC_SUBST(dbdir)
 AC_DEFINE_UNQUOTED([SWITCH_DB_DIR],"${dbdir}",[where to put db files])
 
-htdocsdir="${prefix}/htdocs"
+htdocsdir="${fsdatadir}/htdocs"
 AC_SUBST(htdocsdir)
 AC_DEFINE_UNQUOTED([SWITCH_HTDOCS_DIR],"${htdocsdir}",[where to put htdocs files])
 
-soundsdir="${prefix}/sounds"
+soundsdir="${fsdatadir}/sounds"
 AC_SUBST(soundsdir)
 AC_DEFINE_UNQUOTED([SWITCH_SOUNDS_DIR],"${soundsdir}",[where to put sounds files])
 
-grammardir="${prefix}/grammar"
+grammardir="${fsdatadir}/grammar"
 AC_SUBST(grammardir)
 AC_DEFINE_UNQUOTED([SWITCH_GRAMMAR_DIR],"${grammardir}",[where to put grammar files])
 
-scriptdir="${prefix}/scripts"
+scriptdir="${fsdatadir}/scripts"
 AC_SUBST(scriptdir)
 AC_DEFINE_UNQUOTED([SWITCH_SCRIPT_DIR],"${scriptdir}",[where to put script files])
 
-recordingsdir="${prefix}/recordings"
+recordingsdir="${fsdatadir}/recordings"
 AC_SUBST(recordingsdir)
 AC_DEFINE_UNQUOTED([SWITCH_RECORDINGS_DIR],"${recordingsdir}",[where to put recording files])
 
